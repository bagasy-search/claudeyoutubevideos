// MovS12Cierre.tsx — MOVIMIENTO S12 · "NO TE VAYAS CON LAS MANOS VACÍAS"
// Video `cmepanel30` (Claudio Mendoza Constructor, ES). 5 actos · 0 → 1875 frames @30 = 62,50 s.
// Tramo global 38535 → 40410 (frame local = global − 38535). Se monta ENCIMA del avatar real.
//
// ⛔⛔ EL TONO — esto NO es un anuncio. Es un tipo que abre su cuaderno de dos años sobre la mesa
// del taller y dice "lo dejé abajo por si te sirve".
//   · CERO precio en pantalla. CERO "compra". CERO "oferta". CERO urgencia. CERO botón.
//   · La URL no se dice ni se escribe. Sólo el QR, y APOYADO EN LA MADERA como un objeto más.
//   · Las cifras que sí aparecen (36 · 13,50 · 11,48 · 5 · 45) son AHORRO MENSUAL — lo que la casa
//     te devuelve —, nunca el precio de nada. Por eso la columna se titula "LO QUE TE DEVUELVE AL
//     MES" y por eso entran CÁLIDAS Y DESDE ABAJO (`flujo("queda")`, la ley de dirección de la §5).
//   · El remate del acto 4 es el argumento generoso del guion: esos 45 al mes son MÁS de lo que le
//     ahorra a él su propio panel. Él te está diciendo que probablemente no necesitás comprar nada.
//     Ese es el tono, y por eso el movimiento cierra con "SIN COMPRAR NADA", no con un CTA.
//   · ⛔ El número 320 (lo que le costó el panel) NO se escribe: es un precio y acá no va ninguno.
//     Lo dice la voz; la pantalla se queda con la comparación, no con la cifra.
//
// ── LA TÉCNICA CENTRAL ────────────────────────────────────────────────────────────────────────
// Las seis láminas `cmep30_s12_lam_*` están dibujadas con FILAS MANUSCRITAS ILEGIBLES a propósito
// (ningún motor de imagen escribe texto real). Entonces: **la materia es la foto, el contenido es
// nuestro**. La tabla de las 14 acciones NO es una infografía flotando en el vacío: es la HOJA
// REAL, con su grano y su sombra sobre la madera del banco, y encima se componen — con la
// tipografía del kit — las cuatro filas y su columna de dinero. Se lee como papel, no como código.
//
// ── LA APERTURA DEL AVATAR: **LA ESCUADRA** (gesto nuevo) ─────────────────────────────────────
// Los hermanos ya usaron bandas laterales que se juntan, guillotina, telón, persiana, escalonada,
// trapecio con borde inclinado, visera y tronera. Éste es otro: el hueco es una **ESCUADRA DE
// CARPINTERO** (una "L" acostada) — primero se abre una FRANJA BAJA Y ANCHA (la tabla del banco) y
// después, sólo en W2, de esa franja SUBE UN BRAZO VERTICAL angosto por la izquierda (x 12-28 %),
// como el otro lado de la escuadra apoyado contra el marco.
// ⚠️ EN ESTE TRAMO EL AVATAR VA EN BUCLE Y SU LIPSYNC NO COINCIDE CON LA VOZ. Por eso la franja
// arranca en el 66-68 % de altura: se ven EL BANCO, EL TORSO Y LAS MANOS, **nunca la boca**. Y el
// brazo vertical sube por FUERA de la caja de la cara (30-70 % en x): es un pedazo del garaje real
// al costado de él, no un primer plano suyo hablando. Total de avatar visible en 62 s: 294 frames,
// siempre sin boca. El resto del movimiento él es fondo garantizado, tapado al 100 %.
//
// ══════════════════════════════════════════════════════════════════════════════════════════════
// TABLA DE HANDOFF
// ══════════════════════════════════════════════════════════════════════════════════════════════
// ⇐ ENTRA DESDE los tres números que hay que anotar (S11):
//   cam {encuadre CERRADO sobre la hoja con anotaciones, deriva hacia la DERECHA, grúa −34, push 1,10}
//   luz {HORA.papel 70°, blanco, amb 0,88 (rampa 0,80 → 0,88 en 12 frames)}
//   materia {LA HOJA CON LAS TRES FILAS — `cmep30_s11_clip_escribe_tres.mp4`, él dibujando las tres
//            rayas una debajo de la otra}
//
// ACTO 1 · 0-423 · "NO TE VAYAS CON LAS MANOS VACÍAS"   protagonista: EL CUADERNO DE DOS AÑOS
//   entra  cam {grúa −34, push 1,10, foco 42/58, derivando a la derecha}   luz {papel 70°, amb 0,88}
//          materia {la hoja de las tres filas, en plano cerrado}
//   sale   cam {grúa +18, push 1,00, foco 50/48}                           luz {72°, amb 0,88}
//          materia {EL CUADERNO ESPIRALADO ABIERTO junto a la hoja limpia — `lam_cuaderno_pasado_limpio`}
//   (la ventana W1 · LA TABLA abre 118-316: sólo la franja baja. Se ven sus manos sobre el banco
//    mientras dice "todo lo que anoté en estos dos años con la pinza". Ni un frame de su boca.
//    El cambio de cama — del clip de la escritura a la pila de hojas de canto — pasa TAPADO por la
//    propia apertura de la franja: la geometría hace de costura.)
//   ── FRONTERA A @423 ···· MATCH-SHAPE: el cuaderno espiralado gastado y la hoja limpia están en
//      la MISMA foto, uno al lado del otro. La tarjeta que lo contiene crece de 470×282 a
//      1460×822, el radio baja de 14 a 3, y la superficie SE REPINTA de izquierda a derecha con un
//      borde DURO y su especular montado encima: el cuaderno de dos años se vuelve LA HOJA PASADA
//      EN LIMPIO. El objeto nunca suelta el cuadro y en cada frame hay una línea neta entre las dos
//      materias. ⛔ Jamás un fundido. ·····································································
// ACTO 2 · 423-817 · "14 COSAS, SIN COMPRAR NADA"       protagonista: LA HOJA ENTERA
//   entra  cam {grúa +18 → +24, push 1,00 → 1,03, foco 50/48}   luz {72°, blanco, amb 0,88}
//          materia {la hoja cenital a sangre, el grano del papel y la veta de la madera}
//   sale   cam {grúa +2, push 1,05, foco 50/50}                 luz {76°, amb 0,84}
//          materia {LA HOJA, ya en pie, empezando a cruzar el cuadro}
//   (las 14 rayas del renglonado son GRÁFICA DE APOYO dibujada SOBRE la foto real de la hoja —
//    nunca un vector haciendo de objeto. 11 se encienden CÁLIDAS desde abajo porque son lo que te
//    queda; las 3 restantes se quedan en el gris del papel: la cuenta se VE, no se escribe. Y el
//    desmarque: el foco de "apagá la luz al salir" entra y una barra de HORMIGÓN lo tacha.)
//   ── FRONTERA B @817 ···· OCLUSIÓN: LA HOJA cruza de derecha a izquierda con su doblez, su fibra
//      y su sombra de contacto y tapa el 100 % entre los frames 814 y 823 — justo adentro de "Son
//      estas.". El acto cambia AHÍ DENTRO. El color es el del PAPEL (#EFE8D6), nunca el del fondo:
//      con el del fondo esto es un fundido a negro que se ve. ············································
// ACTO 3 · 817-1427 · ⭐ LAS CUATRO CON SU CIFRA        protagonista: LA HOJA ESCRITA + SUS 4 CLIPS
//   entra  cam {grúa +8, push 1,06, foco 40/40}                 luz {76° → 70°, amb 0,84 → 0,82}
//          materia {la hoja apoyada en la madera con su mano al costado — `lam_mano_al_costado`}
//   sale   cam {grúa +60, push 1,03, foco 62/62 — la cámara BAJÓ por la hoja mientras se escribía}
//          luz {48°, ámbar creciendo, amb 0,74}
//          materia {LA COLUMNA DE DINERO: las cuatro cifras juntándose en un solo bloque}
//   (UNA sola cama para 610 frames: contención. La variedad sale del material y de la escala, no de
//    meter elementos. Cada acción entra EN EL FRAME EXACTO de su palabra y su cifra cae EN EL FRAME
//    EXACTO en que la voz la dice: 848/924 · 970/1157 · 1199/1272 · 1322/1397. Las filas NO se van:
//    quedan escritas en la hoja, así que cada una se lee entre 500 y 600 frames — muy por encima
//    del piso de lectura.)
//   (costura INTERNA ×5 ···· MATCH-MOVE: las tarjetas con los clips reales de él haciendo cada
//    acción viajan sobre UN SOLO RIEL (`rielOff`): la que se va y la que llega comparten la misma
//    función de desplazamiento, o sea la misma velocidad, la misma dirección y la misma distancia
//    (102 % de pantalla). Es una cinta transportadora de cuatro acciones, no cinco entradas
//    sueltas. Swaps @862 · 964 · 1062 · 1192 · 1290 · 1392, y ninguna tarjeta de VIDEO vive mas
//    de 140 frames: los clips de agnes duran 5,04 s = 151 frames a 30 fps.)
//   ── FRONTERA C @1427 ···· ZOOM-THROUGH: las cuatro cifras convergen a una columna apretada entre
//      1404 y 1428 y la cámara SE METE dentro de ella (fx 62 / fy 54); sale del otro lado ya sobre
//      el banco con la calculadora encima de la hoja. La cama de la calculadora se monta 18 f ANTES
//      (@1409) y por DEBAJO: un zoom-through contra nada es un fundido a negro. ·······················
// ACTO 4 · 1427-1548 · "45 DÓLARES AL MES"              protagonista: EL 45
//   entra  cam {grúa +44, push 1,20 (salimos empujados del zoom), foco 62/56}   luz {44°, amb 0,72}
//          materia {la calculadora sobre la hoja: "es aritmética, no es magia"}
//   sale   cam {grúa +6, push 1,08, foco 50/52}                 luz {34°, ámbar, amb 0,68}
//          materia {LOS TRES CUADERNILLOS DE CARTÓN, ya juntándose en una pila}
//   (el 45 sube CÁLIDO desde abajo — `flujo("queda")` literal — porque es lo que te queda, no lo que
//    te cobran. Y el remate honesto: es MÁS de lo que le ahorra su propio panel. Ese cartel
//    sobrevive la frontera D y muere ya adentro del acto 5, que es donde la idea cierra.)
//   (la ventana W2 · LA ESCUADRA abre 1448-1546: la franja baja otra vez y, saliendo de ella, el
//    brazo vertical a su izquierda. Es un instante — 56 frames de brazo — y nunca su boca.)
//   ── FRONTERA D @1548 ···· WIPE POR MATERIA: los TRES CUADERNILLOS ENGRAPADOS cruzan el cuadro de
//      canto — cartón manila (#C9B98F) con sus grapas, el canto de las hojas y su sombra — y detrás
//      ya está el banco del cierre. Es literalmente lo que él está apilando en el plano, no un
//      efecto inventado. El color es el del CARTÓN, jamás el del fondo. ································
// ACTO 5 · 1548-1875 · EL CUADERNO Y EL QR              protagonista: EL QR APOYADO EN LA MADERA
//   entra  cam {grúa +6 → −12 (la cámara SUBE mientras el cartón termina de cruzar), push 1,08 → 1,04}
//          luz {34° → 26°, ámbar, amb 0,68 → 0,64}
//          materia {los tres cuadernillos aterrizando en la madera del banco}
//   sale   cam {grúa −16, push 1,03, foco 40/46 — la cámara YA SE POSÓ: el plano final es un banco
//          quieto con la luz del portón abriéndose}   luz {HORA.cierre 12°, ámbar, amb 0,58}
//          materia {EL CUADERNO CERRÁNDOSE SOBRE LA MESA (la tarjeta se acuesta a rx 58°)}
//   ⭐ EL QR: `img/cmepanel30/cmep30_qrcard.png`, 470 px = 43,5 % del alto del cuadro (la mancha de
//     tinta sola mide ~279 px = 25,8 %, o sea ~7,5 px por módulo). Se monta @1548 DETRÁS del wipe de
//     cartón — el cartón lo DESCUBRE ya apoyado en la mesa, no lo trae volando — y queda hasta 1875.
//     ⛔ Vive FUERA de `Layers` (fuera de la cámara): sin perspectiva, sin rotación, sin escala
//     animada, sin blur, sin grade, sin deriva y sin una sola capa encima. Es el ÚLTIMO nodo del
//     árbol. Lo único que lo acompaña es su sombra de contacto sobre la veta, que va DEBAJO, y su
//     rótulo, que va DEBAJO de la caja. Ningún otro elemento del acto 5 entra nunca en x 60-87 % ·
//     y 32-83 %.
//     **VENTANA LIMPIA GARANTIZADA: 1596 → 1875 = 279 frames (9,30 s).** El wipe termina en 1592 y
//     el polvo se apaga en 1560.
//     El "hold vivo" del acto lo ponen la cama, la luz que cae a HORA.cierre, el polvo previo y el
//     cuaderno que se cierra a la izquierda: el QR es un objeto EN REPOSO sobre una mesa, y un
//     objeto en reposo no tiembla.
//
// ⇒ SALE HACIA el remate final (Carlos + el pedido de que escriban su consumo de fondo):
//   cam {ABIERTA sobre el banco del garaje, cámara ya arriba y posada, deriva viva sin avanzar}
//   luz {HORA.cierre 12°, ámbar, amb 0,58}   materia {el cuaderno cerrado sobre la mesa}
//
// ⛔ cero Math.random/Date · cero backdrop-filter · cero blur grande full-screen · cero fade en
// ninguna frontera · las cuatro costuras son distintas y no se repiten dos seguidas · cero capa de
// color con opacidad sobre el avatar · TODA tarjeta flotante lleva FOTO o CLIP REAL adentro
// (`MediaCard`); las rayas del renglonado, la columna de dinero y la barra de hormigón son GRÁFICA
// de apoyo dibujada sobre material real, nunca hacen de objeto.
// ⛔ Rutas LITERALES en el punto de uso: el build escanea este .tsx por TEXTO y una ruta armada por
// template literal no viaja en el tar → 404 → chunk muerto con un error que miente.
// ⚠️ Ningún `kind="video"` se monta más de 140 frames (los clips duran 5,04 s = 151 f a 30 fps).
import React from "react";
import { AbsoluteFill, Easing, Img, interpolate, staticFile, useCurrentFrame } from "remotion";
import {
  V, F_DISPLAY, F_BODY, clamp01, lerp, rgba, rnd,
  gcam, light, VoltAtmos, Layers, Plane, MediaCard, PhotoPlane, IconPng,
  Readout, zoomThrough, SunKey, HORA, flujo, Bed,
} from "./PanelStage";

// ── EL RELOJ DEL MOVIMIENTO (frames locales, 30 fps, anclados al ms de Whisper) ───────────────
const A1 = 0;      // 38535 · "Y si hiciste las cuentas... no te vayas con las manos vacías"
const A2 = 423;    // 38958 · "Hay 14 cosas que le bajan la factura... sin comprar absolutamente nada"
const A2B = 616;   // 39151 · "Y 11 de las 14 son gratis, y no son consejos de esos de apagar la luz"
const A3 = 817;    // 39352 · "Son estas."
const R1L = 848;   // 39383 · "Dejar de calentar con estufa eléctrica."
const R1N = 924;   // 39459 · "36 dólares al mes."
const R2L = 970;   // 39505 · "Encender el calefactor solamente en el cuarto donde estás"
const R2N = 1157;  // 39692 · "13,50."
const R3L = 1199;  // 39734 · "Cambiar 10 focos viejos por LED."
const R3N = 1272;  // 39807 · "11,48."
const R4L = 1322;  // 39857 · "Subir el aire acondicionado." (los 3 grados caen en 1372)
const R4N = 1397;  // 39932 · "5 dólares."
const A4 = 1427;   // 39962 · "En una casa común eso suma unos 45 dólares por mes."
const A5 = 1548;   // 40083 · el cierre: el cuaderno y el QR
const G_END = 1875;

// los frames en que el VISUAL cambia de acto: caen ADENTRO de la costura, nunca en el beat exacto
const SW3 = A3 + 2;    // 819  · adentro de la oclusión de la hoja (cobertura 814-823)
const SW5 = A5 + 22;   // 1570 · adentro del wipe de cartón (cobertura 1566-1578)

const CL = { extrapolateLeft: "clamp", extrapolateRight: "clamp" } as const;
const es = (t: number) =>
  interpolate(clamp01(t), [0, 1], [0, 1], { easing: Easing.bezier(0.22, 0.61, 0.28, 1) });
const esOut = (t: number) =>
  interpolate(clamp01(t), [0, 1], [0, 1], { easing: Easing.bezier(0.42, 0, 0.24, 1) });
const esSnap = (t: number) =>
  interpolate(clamp01(t), [0, 1], [0, 1], { easing: Easing.bezier(0.08, 0.86, 0.2, 1) });

// LAS MATERIAS de las costuras. ⛔ Ninguna es el color del fondo.
const PAPEL = "#EFE8D6";     // la hoja pasada en limpio (frontera B)
const CARTON = "#C9B98F";    // la tapa engrapada de los cuadernillos (frontera D)
const HORMIGON = "#8E8C81";  // la barra que tacha el consejo viejo
const MADERA = "#6B5333";    // la veta del banco: ahí aterriza la sombra de contacto del QR

// ── EL RIEL DE LAS CUATRO ACCIONES (costura interna ×5 · MATCH-MOVE) ─────────────────────────
// UNA sola función de desplazamiento. La tarjeta que SE VA y la que LLEGA la comparten, así que
// viajan a la misma velocidad, en la misma dirección y la misma distancia: es una cinta, no cinco
// entradas sueltas. `sale` va de 82 % a −20 %; `entra` va de 184 % a 82 %.
const RIEL_DUR = 34;
const SLOT_X = 82;
const rielOff = (g: number, s: number) => -102 * es(clamp01((g - s) / RIEL_DUR));

// ══ LA APERTURA · **LA ESCUADRA** ═════════════════════════════════════════════════════════════
// El hueco es una "L" acostada: una FRANJA baja (bL..bR desde bTop hasta el borde inferior) y, en
// W2, un BRAZO vertical angosto (cL..cR desde cTop hasta bTop) por fuera de la caja de la cara.
type Cover = { bL: number; bR: number; bTop: number; cL: number; cR: number; cTop: number; open: boolean };
const CERRADO: Cover = { bL: 50, bR: 50, bTop: 100, cL: 50, cR: 50, cTop: 100, open: false };

const coverAt = (g: number): Cover => {
  // W1 · LA TABLA — sólo la franja baja. Se ven el banco, el torso y las manos; nunca la boca.
  // Abre en 118 (sobre "no te vayas con las manos vacías") y cierra ENCOGIÉNDOSE HACIA LA ESQUINA
  // INFERIOR IZQUIERDA: el borde derecho barre hacia adentro mientras el techo de la franja cae.
  if (g >= 118 && g < 316) {
    const techo = esOut(clamp01((g - 118) / 30));
    const abreL = es(clamp01((g - 118) / 38));
    const abreR = es(clamp01((g - 118) / 32));
    const barre = es(clamp01((g - 262) / 40));
    const cae = esOut(clamp01((g - 276) / 36));
    const bTop = lerp(lerp(102, 68, techo), 100.5, cae);
    const bL = lerp(50, 13, abreL);
    const bR = lerp(lerp(50, 87, abreR), 17, barre);
    return { bL, bR, bTop, cL: bL, cR: bL, cTop: bTop, open: bTop < 99 && bR - bL > 1.5 };
  }
  // W2 · LA ESCUADRA COMPLETA — la franja y, saliendo de ella, el brazo vertical a su izquierda
  // (x 12-28 %: fuera de la caja de la cara). El brazo sube, se sostiene, se hunde de vuelta en la
  // franja, y recién ahí la franja se cierra juntando sus dos extremos.
  if (g >= 1448 && g < 1546) {
    const techo = esOut(clamp01((g - 1448) / 28));
    const abre = esSnap(clamp01((g - 1448) / 32));
    const cierra = es(clamp01((g - 1518) / 26));
    const sube = esOut(clamp01((g - 1478) / 26));
    const baja = es(clamp01((g - 1512) / 22));
    const alto = esOut(clamp01((g - 1530) / 16));
    const bTop = lerp(lerp(102, 66, techo), 101, alto);
    const bL = lerp(lerp(50, 12, abre), 50, cierra);
    const bR = lerp(lerp(50, 92, abre), 50, cierra);
    const cTop = lerp(lerp(bTop, 26, sube), bTop, baja);
    const brazo = bL < 13.5 && cTop < bTop - 1.5;
    return {
      bL, bR, bTop,
      cL: brazo ? 13 : bR, cR: brazo ? 28 : bR, cTop: brazo ? cTop : bTop,
      open: bTop < 99 && bR - bL > 1.5,
    };
  }
  return CERRADO;
};

// El polígono recorre el rectángulo entero y se mete en la muesca por el borde INFERIOR.
// Con el brazo cerrado (cL === cR) sus cuatro puntos colapsan sobre el techo de la franja y la
// forma vuelve a ser una franja limpia.
const clipOf = (c: Cover) => {
  if (!c.open) return "inset(0px)";
  const p = (x: number, y: number) => `${x.toFixed(2)}% ${y.toFixed(2)}%`;
  return (
    `polygon(0% 0%, 100% 0%, 100% 100%, ${p(c.bR, 100)}, ${p(c.bR, c.bTop)}, ` +
    `${p(c.cR, c.bTop)}, ${p(c.cR, c.cTop)}, ${p(c.cL, c.cTop)}, ${p(c.cL, c.bTop)}, ` +
    `${p(c.bL, c.bTop)}, ${p(c.bL, 100)}, 0% 100%)`
  );
};

// ── EL CANTO DE LA ESCUADRA. Se dibuja DENTRO del mundo recortado, así que ni un píxel de brillo
//    puede entrar en el hueco: la luz lame el borde desde afuera, jamás desde adentro. ─────────
const CantoEscuadra: React.FC<{ c: Cover; hot: number }> = ({ c, hot }) => {
  const pts =
    `${(c.bL * 19.2).toFixed(1)},1080 ${(c.bL * 19.2).toFixed(1)},${(c.bTop * 10.8).toFixed(1)} ` +
    `${(c.cL * 19.2).toFixed(1)},${(c.bTop * 10.8).toFixed(1)} ${(c.cL * 19.2).toFixed(1)},${(c.cTop * 10.8).toFixed(1)} ` +
    `${(c.cR * 19.2).toFixed(1)},${(c.cTop * 10.8).toFixed(1)} ${(c.cR * 19.2).toFixed(1)},${(c.bTop * 10.8).toFixed(1)} ` +
    `${(c.bR * 19.2).toFixed(1)},${(c.bTop * 10.8).toFixed(1)} ${(c.bR * 19.2).toFixed(1)},1080`;
  return (
    <svg width="100%" height="100%" viewBox="0 0 1920 1080" preserveAspectRatio="none"
      style={{ position: "absolute", inset: 0, pointerEvents: "none" }}>
      {/* el derrame ancho: como vive dentro del clip del mundo, sólo se ve su mitad de AFUERA */}
      <polyline points={pts} fill="none" stroke={rgba(V.bone, 0.09 * hot)} strokeWidth={100} />
      <polyline points={pts} fill="none" stroke={rgba(V.volt, 0.16 * hot)} strokeWidth={34} />
      {/* el filo: la línea neta que separa el mundo del hueco */}
      <polyline points={pts} fill="none" stroke={rgba(V.bone, 0.66 * hot)} strokeWidth={3} />
    </svg>
  );
};

// ── FRONTERA B · OCLUSIÓN — LA HOJA PASADA EN LIMPIO ─────────────────────────────────────────
// Una sola hoja con su doblez, su fibra y su sombra de contacto. Cruza de DERECHA a IZQUIERDA y
// tapa el 100 % entre 814 y 823. Color = PAPEL, jamás el del fondo.
const OcluyeHoja: React.FC<{ at: number; dur?: number }> = ({ at, dur = 32 }) => {
  const frame = useCurrentFrame();
  const p = clamp01((frame - at) / dur);
  if (p <= 0 || p >= 1) return null;
  const L = lerp(112, -178, esOut(p));
  return (
    <AbsoluteFill style={{ overflow: "hidden", pointerEvents: "none" }}>
      <div style={{
        position: "absolute", top: "-24%", left: `${L.toFixed(2)}%`, width: "172%", height: "150%",
        transform: `rotate(${(5 - p * 8).toFixed(2)}deg)`,
        background:
          `linear-gradient(92deg, ${rgba(V.white, 0.62)} 0%, rgba(255,255,255,0) 2.2%),` +
          `linear-gradient(172deg, #FCF8EE 0%, ${PAPEL} 36%, #DDD5BF 72%, #C3BBA5 100%)`,
        boxShadow: `0 0 150px ${rgba(V.ink0, 0.92)}`,
        overflow: "hidden",
      }}>
        {/* el DOBLEZ: una cresta de luz y su valle. Sin esto es un rectángulo claro, no una hoja */}
        <div style={{
          position: "absolute", left: "56%", top: "-10%", width: 28, height: "120%",
          transform: "rotate(-5deg)",
          background: `linear-gradient(90deg, ${rgba(V.ink0, 0.14)}, ${rgba(V.white, 0.86)} 48%, ${rgba(V.ink0, 0.1)})`,
        }} />
        {/* la FIBRA del papel: rayado finísimo, lo que separa "hoja" de "rectángulo claro" */}
        <div style={{
          position: "absolute", inset: 0, opacity: 0.32,
          backgroundImage:
            `repeating-linear-gradient(91deg, ${rgba(V.concrete, 0.1)} 0px, ${rgba(V.concrete, 0.1)} 1px, rgba(0,0,0,0) 1px, rgba(0,0,0,0) 10px)`,
        }} />
        {/* la sombra de contacto del canto que viene entrando */}
        <div style={{
          position: "absolute", left: 0, top: 0, bottom: 0, width: 190,
          background: `linear-gradient(270deg, rgba(0,0,0,0), ${rgba(V.ink0, 0.36)})`,
        }} />
      </div>
    </AbsoluteFill>
  );
};

// ── FRONTERA D · WIPE POR MATERIA — LOS TRES CUADERNILLOS ────────────────────────────────────
// Es lo que él está apilando en el plano anterior (`cmep30_s12_clip_apila_manuales.mp4`): cartón
// manila engrapado que cruza de canto. Materia CARTÓN, nunca el color del fondo.
const Cuadernillos: React.FC<{ at: number; dur?: number }> = ({ at, dur = 44 }) => {
  const frame = useCurrentFrame();
  const p = clamp01((frame - at) / dur);
  if (p <= 0 || p >= 1) return null;
  const tapa = Math.sin(clamp01(p) * Math.PI);
  return (
    <AbsoluteFill style={{ overflow: "hidden", pointerEvents: "none" }}>
      {Array.from({ length: 7 }, (_, i) => {
        const o = rnd(i * 5.7);
        const q = clamp01(p * 1.36 - o * 0.24);
        const x = lerp(-124, 132, esOut(q));
        const w = 620 + o * 640;
        const h = 700 + rnd(i * 9.3) * 420;
        const y = -16 + rnd(i * 2.9) * 58;
        const tilt = -4 + o * 9;
        return (
          <div key={i} style={{
            position: "absolute", left: `${x.toFixed(2)}%`, top: `${y.toFixed(1)}%`,
            width: w, height: h,
            transform: `rotate(${tilt.toFixed(2)}deg)`,
            background:
              `linear-gradient(99deg, ${rgba(CARTON, 0)} 0%, ${CARTON} 6%, #E0D2AB 30%, ` +
              `#AD9C74 62%, ${CARTON} 84%, ${rgba(CARTON, 0)} 100%)`,
            boxShadow: `0 30px 96px ${rgba(V.ink0, 0.76)}`,
            borderRadius: 4,
          }}>
            {/* el canto de las hojas de adentro: rayado finito en el borde inferior */}
            <div style={{
              position: "absolute", left: 0, right: 0, bottom: 0, height: 26, opacity: 0.6,
              backgroundImage:
                `repeating-linear-gradient(90deg, ${rgba(V.ink0, 0.16)} 0px, ${rgba(V.ink0, 0.16)} 1px, rgba(0,0,0,0) 1px, rgba(0,0,0,0) 5px)`,
            }} />
            {/* las dos grapas del lomo */}
            <div style={{
              position: "absolute", left: 34, top: "30%", width: 44, height: 7, borderRadius: 2,
              background: "linear-gradient(180deg, #E6E4DB, #8C8A80)",
              boxShadow: `0 2px 5px ${rgba(V.ink0, 0.6)}`,
            }} />
            <div style={{
              position: "absolute", left: 34, top: "66%", width: 44, height: 7, borderRadius: 2,
              background: "linear-gradient(180deg, #E6E4DB, #8C8A80)",
              boxShadow: `0 2px 5px ${rgba(V.ink0, 0.6)}`,
            }} />
          </div>
        );
      })}
      {/* el instante de cobertura total: el cartón llena el cuadro y ahí adentro cambia el acto */}
      <AbsoluteFill style={{ background: rgba(CARTON, 0.94 * clamp01(tapa * 1.9 - 0.74)) }} />
    </AbsoluteFill>
  );
};

// ── TIPOGRAFÍA propia del movimiento ─────────────────────────────────────────────────────────
const Rotulo: React.FC<{
  children: React.ReactNode; x: number; y: number; color?: string; size?: number; op?: number;
  align?: "left" | "center" | "right";
}> = ({ children, x, y, color = V.bone, size = 32, op = 1, align = "center" }) => (
  <div style={{
    position: "absolute", left: `${x}%`, top: `${y}%`,
    transform: `translate(${align === "left" ? "0%" : align === "right" ? "-100%" : "-50%"},-50%)`,
    opacity: op,
    fontFamily: F_DISPLAY, fontWeight: 700, fontSize: size, letterSpacing: 3.2, color,
    textTransform: "uppercase", whiteSpace: "nowrap", textShadow: "0 4px 20px rgba(0,0,0,0.95)",
  }}>{children}</div>
);

// cartel que ATERRIZA: entra con desplazamiento y escala, nunca arranca de cero
const Cartel: React.FC<{
  g: number; at: number; out: number; x: number; y: number; size?: number; color?: string;
  children: React.ReactNode;
}> = ({ g, at, out, x, y, size = 54, color = V.white, children }) => {
  const inP = es(clamp01((g - at) / 14));
  const outP = clamp01((g - out) / 16);
  if (g < at || outP >= 1) return null;
  return (
    <div style={{
      position: "absolute", left: `${x}%`, top: `${y}%`,
      transform: `translate(-50%,-50%) translateY(${((1 - inP) * 26 + outP * 22 + Math.sin(g / 57) * 2.1).toFixed(1)}px) ` +
        `scale(${(0.93 + 0.07 * inP - outP * 0.05).toFixed(3)})`,
      opacity: Math.min(1, inP * 1.7) * (1 - outP),
    }}>
      <Bed pad={20}>
        <div style={{
          fontFamily: F_DISPLAY, fontWeight: 700, fontSize: size, letterSpacing: 1.6, color,
          textTransform: "uppercase", lineHeight: 1.06, whiteSpace: "nowrap",
          textShadow: "0 5px 22px rgba(0,0,0,0.95)",
        }}>{children}</div>
      </Bed>
    </div>
  );
};

// ── ⭐ LAS 14 RAYAS DEL RENGLONADO (acto 2) ──────────────────────────────────────────────────
// GRÁFICA DE APOYO dibujada SOBRE la foto real de la hoja: nunca un vector haciendo de objeto.
// 11 se encienden CÁLIDAS Y DESDE ABAJO (`flujo("queda")`, lo que te queda); las otras 3 se quedan
// en el gris del papel. La cuenta se VE, no se escribe.
const Renglones: React.FC<{ g: number }> = ({ g }) => {
  const nace = es(clamp01((g - 452) / 44));
  const gratis = clamp01((g - A2B) / 70);
  const muere = clamp01((g - 700) / 24);
  if (g < 446 || muere >= 1) return null;
  return (
    <div style={{ position: "absolute", left: "8%", top: "18%", width: "44%", opacity: 1 - muere }}>
      {Array.from({ length: 14 }, (_, i) => {
        const esGratis = i < 11;
        const t0 = i / 16;
        const vive = clamp01((nace - t0) * 5);
        const fl = flujo("queda", clamp01((gratis - t0 * 0.8) * 3.4));
        const on = esGratis ? fl.opacity : 0;
        return (
          <div key={i} style={{
            position: "relative", height: 46, marginBottom: 4,
            opacity: vive,
            transform: `translateX(${((1 - vive) * -34).toFixed(1)}px)`,
          }}>
            <div style={{
              position: "absolute", left: 0, top: 21, height: 5,
              width: `${(58 + rnd(i * 3.7) * 34).toFixed(1)}%`,
              borderRadius: 3,
              background: esGratis
                ? `linear-gradient(90deg, ${rgba(V.amber, 0.24 + 0.66 * on)}, ${rgba(V.amber, 0.06 + 0.3 * on)})`
                : `linear-gradient(90deg, ${rgba(V.concrete, 0.5)}, ${rgba(V.concrete, 0.14)})`,
              boxShadow: on > 0.2 ? `0 0 22px ${rgba(V.amber, 0.34 * on)}` : "none",
              transform: `translateY(${(fl.dy * 0.05 * (1 - on)).toFixed(2)}px)`,
            }} />
            <div style={{
              position: "absolute", left: -44, top: 12,
              fontFamily: F_DISPLAY, fontWeight: 700, fontSize: 30, letterSpacing: 1,
              color: esGratis ? rgba(V.amber, 0.5 + 0.5 * on) : rgba(V.bone, 0.42),
              textShadow: "0 3px 14px rgba(0,0,0,0.9)",
            }}>{i + 1}</div>
          </div>
        );
      })}
    </div>
  );
};

// ── ⭐⭐ LA HOJA ESCRITA (acto 3) — una fila de la tabla ──────────────────────────────────────
// La materia es la FOTO de la hoja; el contenido lo componemos nosotros con la tipografía del kit.
// El rótulo entra CÁLIDO Y DESDE ABAJO porque es lo que TE QUEDA (`flujo("queda")`, ley de la §5).
// La cifra cae en el frame exacto en que la voz la dice, y no se va más: queda escrita en la hoja.
// `conv` (0→1, frames 1404-1428) junta las cuatro cifras en una columna apretada y apaga el resto:
// eso es la materia que la cámara atraviesa en la frontera C.
const FilaTabla: React.FC<{
  g: number; y: number; convY: number; conv: number; atL: number; atN: number;
  label: string; cifra: string; icono: React.ReactNode;
}> = ({ g, y, convY, conv, atL, atN, label, cifra, icono }) => {
  if (g < atL - 2) return null;
  const fl = flujo("queda", clamp01((g - atL) / 22));
  const raya = es(clamp01((g - atL - 6) / 26));
  const nPop = clamp01((g - atN) / 8);
  const apaga = 1 - conv;
  return (
    <div style={{
      position: "absolute", left: 0, right: 0, top: `${lerp(y, convY, conv).toFixed(2)}%`,
      transform: `translateY(${(fl.dy * 0.22 + Math.sin(g / 63 + y) * 1.6).toFixed(2)}px)`,
      opacity: fl.opacity,
    }}>
      <div style={{ opacity: apaga }}>{icono}</div>
      {/* el renglón sobre el que está escrita la fila: gráfica de apoyo sobre el papel real */}
      <div style={{
        position: "absolute", left: "9%", top: 34, height: 3,
        width: `${(51 * raya).toFixed(2)}%`, opacity: apaga,
        background: `linear-gradient(90deg, ${rgba(V.amber, 0.34)}, ${rgba(V.amber, 0.06)})`,
      }} />
      <div style={{
        position: "absolute", left: "9%", top: -22, opacity: apaga,
        fontFamily: F_DISPLAY, fontWeight: 700, fontSize: 46, letterSpacing: 1.4,
        color: V.white, textTransform: "uppercase", whiteSpace: "nowrap",
        textShadow: "0 5px 22px rgba(0,0,0,0.96), 0 2px 6px rgba(0,0,0,0.9)",
      }}>{label}</div>
      {/* la cifra: AHORRO mensual, jamás un precio. Cae en el frame exacto de su palabra. */}
      <div style={{
        position: "absolute", left: "60%", top: -34,
        transform: `translateX(-100%) scale(${(1 + (1 - es(nPop)) * 0.18).toFixed(3)})`,
        transformOrigin: "100% 50%",
        opacity: clamp01((g - atN) / 6),
        fontFamily: F_DISPLAY, fontWeight: 800, fontSize: 82, lineHeight: 0.9,
        letterSpacing: 0.5, color: V.amber, whiteSpace: "nowrap",
        textShadow: `0 0 34px ${rgba(V.amber, 0.34 * nPop)}, 0 6px 26px rgba(0,0,0,0.94)`,
      }}>{cifra}</div>
    </div>
  );
};

// ── LA CÁMARA · una sola función de g, con deriva viva, que NUNCA vuelve a cero ───────────────
const KF = [A1, 118, 262, A2, 520, A2B, 700, A3, R1L, R1N, R2L, R2N, R3L, R3N, R4L, R4N,
  A4, 1448, 1520, A5, 1596, 1720, G_END];
const camAt = (g: number) => {
  const base = gcam(g, { z0: -120, z1: 96, panX: 112, panY: -26, ry: 3.6, rx: -1.2, dur: G_END });
  const crane = interpolate(
    g, KF,
    [-34, -10, 6, 18, 24, 14, 2, -6, 8, 16, 22, 34, 40, 48, 54, 60,
      44, 30, 16, 6, -12, -15, -16],
    { ...CL, easing: Easing.bezier(0.4, 0, 0.24, 1) },
  );
  const push = interpolate(
    g, KF,
    [1.10, 1.04, 1.02, 1.00, 1.03, 1.02, 1.05, 1.16, 1.06, 1.03, 1.04, 1.02, 1.04, 1.02, 1.05, 1.03,
      1.20, 1.10, 1.05, 1.08, 1.04, 1.035, 1.03],
    { ...CL, easing: Easing.bezier(0.42, 0, 0.3, 1) },
  );
  const FK = [A1, 262, A2, A2B, A3, R1N, R2N, R3N, R4N, A4, A5, 1700, G_END];
  const fx = interpolate(g, FK, [42, 46, 50, 46, 50, 40, 40, 40, 62, 62, 50, 44, 40], CL);
  const fy = interpolate(g, FK, [58, 52, 48, 46, 50, 40, 48, 56, 54, 56, 52, 50, 46], CL);
  const tx = (50 - fx) * (push - 1);
  const ty = (50 - fy) * (push - 1);
  return (
    `${base.transform} translateY(${crane.toFixed(1)}px) ` +
    `translate(${tx.toFixed(2)}%, ${ty.toFixed(2)}%) scale(${push.toFixed(4)})`
  );
};

// ══════════════════════════════════════════════════════════════════════════════════════════════
export const MovS12Cierre: React.FC<{ acto?: number; gFrame?: number }> = ({ gFrame }) => {
  const cf = useCurrentFrame();
  const g = gFrame === undefined ? cf : gFrame;
  const toCF = (t: number) => cf - g + t;   // pasa un frame mío al reloj de las primitivas del Stage

  // ── LA LUZ: función continua de g. Entra en HORA.papel (70°, blanco, amb 0,88) y termina en
  //    HORA.cierre (12°, ámbar, amb 0,58): el sol BAJA durante los 62 s y el garaje se pone de
  //    tarde. Evoluciona, nunca salta. Rampa de arranque = 12 frames (≤15). ─────────────────
  const LK = [A1, 12, A2, A2B, A3, R2L, R3L, R4N, A4, A5, 1700, G_END];
  const sunAng = interpolate(
    g, LK,
    [HORA.papel.ang, HORA.papel.ang, 72, 74, 76, 70, 60, 48, 44, 34, 22, HORA.cierre.ang], CL,
  );
  const amb = interpolate(
    g, LK,
    [0.80, HORA.papel.amb, 0.88, 0.86, 0.84, 0.82, 0.78, 0.74, 0.72, 0.68, 0.62, HORA.cierre.amb], CL,
  );
  // los tres soles: cálido (lo que te queda) · frío (lo que te cobran) · blanco (el papel a pleno)
  const warmW = interpolate(g, LK, [0.28, 0.30, 0.34, 0.40, 0.44, 0.50, 0.56, 0.64, 0.72, 0.80, 0.88, 0.94], CL);
  const coldW = interpolate(g, LK, [0.34, 0.34, 0.30, 0.26, 0.24, 0.20, 0.18, 0.16, 0.14, 0.12, 0.10, 0.08], CL);
  const dayW = interpolate(g, LK, [0.90, 0.94, 0.96, 0.94, 0.90, 0.84, 0.74, 0.62, 0.56, 0.44, 0.30, 0.20], CL);
  const coolMix = interpolate(g, LK, [0.20, 0.20, 0.16, 0.14, 0.12, 0.12, 0.10, 0.10, 0.10, 0.08, 0.06, 0.05], CL);
  const keyFrom = interpolate(g, LK, [0.30, 0.32, 0.40, 0.46, 0.50, 0.44, 0.38, 0.34, 0.40, 0.46, 0.54, 0.62], CL);
  const inten = interpolate(g, LK, [0.90, 1.00, 1.02, 0.98, 0.94, 0.90, 0.86, 0.82, 0.90, 0.86, 0.80, 0.74], CL);
  const floorDim = interpolate(g, LK, [0.50, 0.50, 0.46, 0.44, 0.46, 0.50, 0.54, 0.58, 0.52, 0.50, 0.56, 0.62], CL);

  const cam = camAt(g);
  const cov = coverAt(g);
  const clip = clipOf(cov);
  const cantoHot = cov.open ? clamp01((cov.bR - cov.bL) / 26) : 0;

  // ── FRONTERA A @423 · MATCH-SHAPE. El cuaderno espiralado y la hoja limpia están en la MISMA
  //    foto. La tarjeta crece, se le achica el radio y la superficie SE REPINTA con un borde duro
  //    que viaja de izquierda a derecha: nunca un fundido. ───────────────────────────────────
  const morf = es(clamp01((g - A2) / 66));
  const mw = lerp(470, 1460, morf);
  const mh = lerp(282, 822, morf);
  const mxIn = es(clamp01((g - 396) / 28));
  const mx = lerp(lerp(118, 33, mxIn), 50, morf);
  const my = lerp(56, 48, morf);
  const sweep = clamp01((g - A2 - 8) / 52);          // el borde neto que repinta la superficie
  const verMorfo = g >= 394 && g < 524;

  // ── FRONTERA C @1427 · ZOOM-THROUGH dentro de la columna de dinero ────────────────────────
  const conv = es(clamp01((g - 1404) / 24));         // las cuatro cifras convergen antes del salto
  const zw = g >= A4 && g < A4 + 30 ? zoomThrough(g, A4, 26, 62, 54) : null;
  const zs: React.CSSProperties = zw
    ? { transform: zw.out, opacity: zw.opacity, transformStyle: "preserve-3d" }
    : { transformStyle: "preserve-3d" };

  // ── EL RIEL de las cuatro acciones (costura interna ×5 · MATCH-MOVE) ──────────────────────
  const S_A = 862, S_B = 964, S_B2 = 1062, S_C = 1192, S_D = 1290, S_OUT = 1392;
  const xEntra = (s: number) => SLOT_X + 102 + rielOff(g, s);
  const xSale = (s: number) => SLOT_X + rielOff(g, s);
  const slotA = g < S_B ? xEntra(S_A) : xSale(S_B);
  const slotB = g < S_B2 ? xEntra(S_B) : xSale(S_B2);
  const slotB2 = g < S_C ? xEntra(S_B2) : xSale(S_C);
  const slotC = g < S_D ? xEntra(S_C) : xSale(S_D);
  const slotD = g < S_OUT ? xEntra(S_D) : xSale(S_OUT);

  // mounts (las camas se montan ANTES de su costura: un zoom-through o un wipe contra nada es un
  // fundido a negro que se ve)
  const bgEscribe = g < 140;
  const bgPila = g >= 132 && g < 476;
  const bgHojaCenital = g >= A2 && g < 836;
  const bgHojaMano = g >= 808 && g < 1458;
  const bgCalculadora = g >= 1409 && g < 1600;
  const bgManuales = g >= SW5 - 30;

  // el QR: se monta detrás del wipe de cartón y se queda. FUERA de la cámara, sin una sola
  // transformación. La ventana LIMPIA garantizada arranca cuando el cartón termina de cruzar.
  const QR_MOUNT = A5;        // 1548 — detrás del wipe
  const QR_CLEAN = 1596;      // desde acá no hay NADA por encima ni cerca de su caja
  const QR_PX = 470;          // 43,5 % del alto del cuadro (la tinta sola ≈ 279 px = 25,8 %)

  return (
    <AbsoluteFill>
      {/* ════ EL MUNDO — todo lo que puede tapar al avatar vive acá dentro, y ESTO es lo único que
          la escuadra recorta. Ni un solo píxel de estas capas llega nunca a su cara. ════ */}
      <AbsoluteFill style={{ clipPath: clip, WebkitClipPath: clip }}>
        {/* la atmósfera se monta UNA vez para los 62 s: nunca se remonta entre actos */}
        <VoltAtmos
          tint={light(coolMix, "volt", "sky")} tint2={V.amber}
          keyFrom={keyFrom} intensity={inten} floor={floorDim}
        />
        {/* el SOL es un personaje: baja de 70° a 12° en los 62 s. Lo CÁLIDO viene de abajo. */}
        <SunKey ang={sunAng} temp="amber" amb={warmW * amb} soft={68} />
        <SunKey ang={96} temp="sky" amb={coldW * amb * 0.7} soft={82} />
        <SunKey ang={sunAng} temp="white" amb={dayW * amb * 0.7} soft={90} />

        <Layers cam={cam}>
          {/* ── ACTO 4 · LA CALCULADORA SOBRE LA HOJA. Se monta 18 f ANTES del zoom-through y por
              DEBAJO (z −920): la cama saliente tiene que quedarle encima o el zoom es un fade. ── */}
          {bgCalculadora && (
            <Plane z={-920}>
              <PhotoPlane src="img/cmepanel30/cmep30_s11_calculadora_papel.png" kind="photo" z={0}
                scale={interpolate(g, [1409, A4, 1480, 1600], [1.86, 1.40, 1.24, 1.18], CL)}
                dim={interpolate(g, [1409, A4, 1480, 1600], [0.34, 0.44, 0.4, 0.5], CL)} tint={V.amber} />
            </Plane>
          )}

          {/* ── ACTO 1a · LA HOJA CON LAS TRES FILAS (materia entrante de S11) ─────────────── */}
          {bgEscribe && (
            <Plane z={-860}>
              <PhotoPlane src="broll/cmepanel30/cmep30_s11_clip_escribe_tres.mp4" kind="video" z={0}
                startFrom={0}
                scale={interpolate(g, [0, 140], [1.30, 1.18], CL)}
                dim={interpolate(g, [0, 40, 116, 140], [0.52, 0.4, 0.46, 0.58], CL)} tint={V.white} />
            </Plane>
          )}
          {/* ── ACTO 1b · LA PILA DE DOS AÑOS, DE CANTO. El cambio de cama va TAPADO por la
              apertura de la franja: la geometría de la escuadra hace de costura. ──────────── */}
          {bgPila && (
            <Plane z={-880}>
              <PhotoPlane src="img/cmepanel30/cmep30_s12_lam_pila_costado.png" kind="photo" z={0}
                scale={interpolate(g, [132, 300, 476], [1.34, 1.22, 1.16], CL)}
                dim={interpolate(g, [132, 200, 320, 476], [0.6, 0.44, 0.4, 0.54], CL)} tint={V.amber} />
            </Plane>
          )}

          {/* ── ACTO 2 · LA HOJA ENTERA, CENITAL, SOBRE LA MADERA DEL BANCO ────────────────── */}
          {bgHojaCenital && (
            <Plane z={-880}>
              <PhotoPlane src="img/cmepanel30/cmep30_s12_lam_cenital_hoja.png" kind="photo" z={0}
                scale={interpolate(g, [A2, 520, A2B, 836], [1.24, 1.16, 1.18, 1.26], CL)}
                dim={interpolate(g, [A2, 470, 560, A2B, 760, 836], [0.62, 0.4, 0.34, 0.4, 0.5, 0.6], CL)}
                tint={V.white} />
            </Plane>
          )}

          {/* ── ACTO 3 · LA HOJA APOYADA EN LA MADERA, CON SU MANO AL COSTADO. UNA sola cama para
              610 frames: contención. Sale ATRAVESADA por la cámara en la frontera C. ──────── */}
          {bgHojaMano && (
            <AbsoluteFill style={zs}>
              <Plane z={-880}>
                <PhotoPlane src="img/cmepanel30/cmep30_s12_lam_mano_al_costado.png" kind="photo" z={0}
                  scale={interpolate(g, [808, 900, R2N, R4N, 1458], [1.34, 1.28, 1.24, 1.21, 1.24], CL)}
                  dim={interpolate(g, [808, 880, R1N, R3N, R4N, 1458], [0.66, 0.5, 0.46, 0.5, 0.54, 0.62], CL)}
                  tint={V.amber} />
              </Plane>
            </AbsoluteFill>
          )}

          {/* ── ACTO 5 · EL BANCO DEL GARAJE AL ATARDECER. La cama se abre (scale bajando) y la luz
              cae a HORA.cierre: eso es "abrir hacia el garaje de tarde". ──────────────────── */}
          {bgManuales && (
            <Plane z={-900}>
              <PhotoPlane src="img/cmepanel30/cmep30_s12_lam_tres_manuales.png" kind="photo" z={0}
                scale={interpolate(g, [SW5 - 30, 1600, 1740, G_END], [1.30, 1.20, 1.17, 1.14], CL)}
                dim={interpolate(g, [SW5 - 30, 1620, 1780, G_END], [0.62, 0.46, 0.44, 0.5], CL)}
                tint={V.amber} />
            </Plane>
          )}
        </Layers>

        {/* el canto de la escuadra: vive DENTRO del clip del mundo, así que su derrame sólo puede
            verse hacia AFUERA. Ni un píxel de brillo entra en el hueco. */}
        {cov.open && <CantoEscuadra c={cov} hot={cantoHot} />}
      </AbsoluteFill>

      {/* ════ EL PRIMER PLANO — no está recortado. Con la escuadra ABIERTA todo vive fuera del
          hueco: nunca sobre su torso, nunca sobre sus manos, nunca sobre la cara. ════ */}
      <Layers cam={cam}>

        {/* ═══ ACTO 1 · "NO TE VAYAS CON LAS MANOS VACÍAS" ═══════════════════════════════════ */}
        {g < 470 && (
          <>
            {/* el titular, arriba: la franja del avatar vive de 68 % para abajo y nada la pisa */}
            <Plane z={220}>
              <Cartel g={g} at={46} out={176} x={50} y={30} size={62} color={V.white}>
                NO TE VAYAS<br />CON LAS MANOS <span style={{ color: V.volt }}>VACÍAS</span>
              </Cartel>
            </Plane>

            {/* con la franja abierta él está abajo, en el banco. Arriba, su cuaderno de dos años:
                material REAL corriendo, no un ícono de cuaderno. */}
            {g >= 182 && g < 314 && (
              <Plane z={180}>
                <MediaCard src="broll/cmepanel30/cmep30_s12_clip_hojea_cuaderno.mp4" kind="video"
                  w={lerp(430, 500, es(clamp01((g - 182) / 80)))}
                  h={lerp(258, 300, es(clamp01((g - 182) / 80)))}
                  x={lerp(-14, 24, es(clamp01((g - 182) / 40)))}
                  y={lerp(34, 30, es(clamp01((g - 182) / 70)))}
                  z={0} ry={13} rx={-2} startFrom={2} lit={1} litColor={V.amber}
                  label="DOS AÑOS ANOTANDO" sheenAt={toCF(244)} radius={12} />
              </Plane>
            )}
            <Plane z={230}>
              <Rotulo x={74} y={22} size={38} color={V.white}
                op={clamp01((g - 196) / 12) * clamp01(1 - (g - 306) / 14)}>
                DOS AÑOS CON <span style={{ color: V.volt }}>LA PINZA</span>
              </Rotulo>
              {g >= 206 && g < 312 && (
                <IconPng src="img/cmepanel30/cmep30_ic_pinza.png" x={74} y={28} size={108} z={0}
                  opacity={0.9 * clamp01((g - 206) / 14) * clamp01(1 - (g - 298) / 12)}
                  rot={-6} glow={V.ink0} />
              )}
            </Plane>

            {/* el gesto de compartir, no de vender: "lo que a mí más me sorprendió".
                Arranca en 316, o sea con la escuadra YA cerrada (coverAt vuelve a CERRADO). */}
            <Plane z={240}>
              <Cartel g={g} at={316} out={418} x={50} y={76} size={58} color={V.white}>
                LO QUE MÁS <span style={{ color: V.amber }}>ME SORPRENDIÓ</span>
              </Cartel>
            </Plane>
          </>
        )}

        {/* ═══ FRONTERA A · MATCH-SHAPE — EL CUADERNO QUE SE VUELVE LA HOJA ══════════════════
            El objeto NO suelta el cuadro: crece, se le achica el radio y la superficie se repinta
            con un borde DURO que viaja. En cada frame hay una línea neta. ⛔ Nunca un fundido. */}
        {verMorfo && (
          <Plane z={130}>
            <div style={{
              position: "absolute", left: `${mx.toFixed(3)}%`, top: `${my.toFixed(3)}%`,
              width: mw, height: mh, marginLeft: -mw / 2, marginTop: -mh / 2,
              transform: `rotateY(${lerp(-11, 0, morf).toFixed(2)}deg) rotateX(${lerp(4, 0, morf).toFixed(2)}deg) ` +
                `translateY(${(Math.sin(g / 47) * 2.4).toFixed(2)}px)`,
              transformStyle: "preserve-3d",
              borderRadius: lerp(14, 3, morf),
              boxShadow: `0 ${Math.round(mh * 0.15)}px ${Math.round(mh * 0.2)}px ${rgba(V.ink0, 0.74)}`,
              overflow: "hidden",
            }}>
              {/* SUPERFICIE 1 · el cuaderno espiralado gastado (la materia que viene del acto 1) */}
              <MediaCard src="img/cmepanel30/cmep30_s12_lam_cuaderno_pasado_limpio.png" kind="photo"
                w={mw} h={mh} x={50} y={50} z={0} lit={1} litColor={V.amber}
                sheenAt={toCF(408)} radius={lerp(14, 3, morf)} />
              {/* SUPERFICIE 2 · la hoja pasada en limpio. Se revela con un BORDE NETO. */}
              {sweep > 0.001 && (
                <div style={{
                  position: "absolute", inset: 0,
                  clipPath: `inset(0% ${((1 - sweep) * 100).toFixed(2)}% 0% 0%)`,
                  WebkitClipPath: `inset(0% ${((1 - sweep) * 100).toFixed(2)}% 0% 0%)`,
                }}>
                  <MediaCard src="img/cmepanel30/cmep30_s12_lam_cenital_hoja.png" kind="photo"
                    w={mw} h={mh} x={50} y={50} z={0} lit={1} litColor={V.white}
                    radius={lerp(14, 3, morf)} />
                </div>
              )}
              {/* el especular montado sobre el borde: la superficie SE REPINTA, no se funde */}
              {sweep > 0.001 && sweep < 0.999 && (
                <div style={{
                  position: "absolute", top: 0, bottom: 0, left: `${(sweep * 100).toFixed(2)}%`,
                  width: 128, marginLeft: -64,
                  background: `linear-gradient(90deg, rgba(255,255,255,0), ${rgba(V.white, 0.5)} 48%, rgba(255,255,255,0))`,
                  mixBlendMode: "screen",
                }} />
              )}
            </div>
          </Plane>
        )}

        {/* ═══ ACTO 2 · "14 COSAS, SIN COMPRAR NADA" ═════════════════════════════════════════ */}
        {g >= 440 && g < 830 && (
          <>
            {/* las 14 rayas del renglonado, dibujadas SOBRE la foto real de la hoja */}
            <Plane z={90}>
              <Renglones g={g} />
            </Plane>

            <Plane z={220}>
              {/* el 14 es una CUENTA, no un precio */}
              {g >= 452 && g < 616 && (
                <div style={{ opacity: clamp01((g - 452) / 8) * clamp01(1 - (g - 598) / 16) }}>
                  <Readout value="14" label="COSAS QUE BAJAN LA FACTURA" at={toCF(455)}
                    x={interpolate(g, [455, 520, 596], [76, 76, 78], CL)}
                    y={interpolate(g, [455, 596], [26, 22], CL)}
                    size={interpolate(g, [455, 500, 596], [200, 152, 146], CL)} color={V.white} />
                </div>
              )}
              <Cartel g={g} at={500} out={616} x={78} y={52} size={56} color={V.white}>
                SIN COMPRAR<br /><span style={{ color: V.volt }}>NADA</span>
              </Cartel>
            </Plane>

            {/* ⭐ 11 DE LAS 14 SON GRATIS. Sube CÁLIDO desde abajo: es lo que te queda. */}
            <Plane z={240}>
              {g >= A2B && g < 800 && (() => {
                const fl = flujo("queda", clamp01((g - A2B) / 26));
                return (
                  <div style={{
                    transform: `translateY(${(fl.dy * 0.5).toFixed(1)}px)`,
                    opacity: fl.opacity * clamp01(1 - (g - 782) / 16),
                  }}>
                    <Readout value="11" label="DE LAS 14 SON GRATIS" at={toCF(A2B + 3)}
                      x={interpolate(g, [A2B, 700, 780], [78, 78, 74], CL)} y={72}
                      size={168} color={V.amber} />
                  </div>
                );
              })()}
              {g >= 636 && g < 792 && (
                <IconPng src="img/cmepanel30/cmep30_ic_moneda.png" x={92} y={58} size={104} z={0}
                  opacity={0.82 * clamp01((g - 636) / 14) * clamp01(1 - (g - 778) / 12)}
                  rot={7} glow={V.ink0} />
              )}
            </Plane>

            {/* EL DESMARQUE: no son los consejos de siempre. El foco entra y una barra de hormigón
                lo tacha — objeto de la escena, no un tachado tipográfico. */}
            {g >= 706 && g < 818 && (() => {
              const tacha = es(clamp01((g - 726) / 18));
              return (
                <Plane z={250}>
                  <IconPng src="img/cmepanel30/cmep30_ic_foco.png" x={22} y={30} size={148} z={0}
                    opacity={0.92 * clamp01((g - 706) / 14) * clamp01(1 - (g - 804) / 12)}
                    rot={-8} glow={V.ink0} />
                  {tacha > 0.01 && (
                    <div style={{
                      position: "absolute", left: "13%", top: "36%", width: `${(19 * tacha).toFixed(2)}%`,
                      height: 13, marginTop: -6,
                      transform: "rotate(-13deg)", transformOrigin: "0% 50%",
                      background: `linear-gradient(180deg, #B7B4A6, ${HORMIGON} 46%, #55544D)`,
                      boxShadow: `0 8px 22px ${rgba(V.ink0, 0.85)}`,
                      opacity: clamp01(1 - (g - 804) / 12),
                    }} />
                  )}
                  <Rotulo x={22} y={50} size={42} color={V.white}
                    op={clamp01((g - 710) / 12) * clamp01(1 - (g - 806) / 12)}>
                    LOS CONSEJOS DE SIEMPRE
                  </Rotulo>
                </Plane>
              );
            })()}
          </>
        )}

        {/* ═══ ACTO 3 · ⭐ LAS CUATRO CON SU CIFRA ════════════════════════════════════════════
            La hoja es la FOTO; la tabla la componemos nosotros. Cada acción entra en el frame de
            su palabra y cada cifra cae en el frame de su número. Las filas NO se van. ═══════ */}
        {g >= SW3 - 10 && g < 1462 && (
          <AbsoluteFill style={zs}>
            {/* "SON ESTAS." — el aviso, ya adentro de la hoja */}
            <Plane z={240}>
              <Cartel g={g} at={824} out={904} x={31} y={15} size={58} color={V.white}>
                SON <span style={{ color: V.volt }}>ESTAS</span>
              </Cartel>
            </Plane>

            {/* la cabecera de la columna: AHORRO, jamás un precio */}
            <Plane z={230}>
              <Rotulo x={60} y={24} size={31} align="right" color={rgba(V.amber, 0.86)}
                op={clamp01((g - 900) / 14) * (1 - conv)}>
                LO QUE TE DEVUELVE AL MES
              </Rotulo>
            </Plane>

            {/* ⭐ LAS CUATRO FILAS ESCRITAS EN LA HOJA. `conv` las junta en la columna que la
                cámara atraviesa en la frontera C. */}
            <Plane z={110}>
              <FilaTabla g={g} y={36} convY={45.5} conv={conv} atL={R1L} atN={R1N}
                label="ESTUFA ELÉCTRICA" cifra="36"
                icono={
                  <div style={{ position: "absolute", left: "5.6%", top: -30 }}>
                    <IconPng src="img/cmepanel30/cmep30_ic_calentador.png" x={0} y={0} size={62}
                      z={0} opacity={0.86} rot={-5} glow={V.ink0} />
                  </div>
                } />
              <FilaTabla g={g} y={49} convY={51} conv={conv} atL={R2L} atN={R2N}
                label="SÓLO EL CUARTO DONDE ESTÁS" cifra="13,50"
                icono={
                  <div style={{ position: "absolute", left: "5.6%", top: -30 }}>
                    <IconPng src="img/cmepanel30/cmep30_ic_casa.png" x={0} y={0} size={62}
                      z={0} opacity={0.86} rot={4} glow={V.ink0} />
                  </div>
                } />
              <FilaTabla g={g} y={62} convY={56.5} conv={conv} atL={R3L} atN={R3N}
                label="10 FOCOS VIEJOS A LED" cifra="11,48"
                icono={
                  <div style={{ position: "absolute", left: "5.6%", top: -30 }}>
                    <IconPng src="img/cmepanel30/cmep30_ic_foco.png" x={0} y={0} size={62}
                      z={0} opacity={0.86} rot={-6} glow={V.ink0} />
                  </div>
                } />
              <FilaTabla g={g} y={75} convY={62} conv={conv} atL={R4L} atN={R4N}
                label="EL AIRE, 3 GRADOS MÁS" cifra="5"
                icono={
                  <div style={{ position: "absolute", left: "5.6%", top: -30 }}>
                    <IconPng src="img/cmepanel30/cmep30_ic_termometro.png" x={0} y={0} size={62}
                      z={0} opacity={0.86} rot={5} glow={V.ink0} />
                  </div>
                } />
            </Plane>

            {/* ── LA CINTA DE LAS CUATRO ACCIONES · costura interna ×5 · MATCH-MOVE ──────────
                Cada tarjeta lleva el CLIP REAL de él haciendo esa acción. La que se va y la que
                llega comparten la misma función de riel: misma velocidad, misma altura. ──── */}
            {g >= S_A && g < S_B + RIEL_DUR && (
              <Plane z={190}>
                <MediaCard src="broll/cmepanel30/cmep30_s12_clip_apaga_estufa.mp4" kind="video"
                  w={440} h={264} x={slotA} y={40} z={0} ry={-10} rx={2}
                  startFrom={2} lit={1} litColor={V.amber}
                  label="APAGAR LA ESTUFA" sheenAt={toCF(886)} radius={12} />
              </Plane>
            )}
            {g >= S_B && g < S_B2 + RIEL_DUR && (
              <Plane z={190}>
                <MediaCard src="broll/cmepanel30/cmep30_s12_clip_cierra_puerta_cuarto.mp4" kind="video"
                  w={440} h={264} x={slotB} y={40} z={0} ry={-10} rx={2}
                  startFrom={2} lit={1} litColor={V.amber}
                  label="CERRAR LA PUERTA" sheenAt={toCF(1024)} radius={12} />
              </Plane>
            )}
            {g >= S_B2 && g < S_C + RIEL_DUR && (
              <Plane z={190}>
                <MediaCard src="img/cmepanel30/cmep30_s12_calefactor_un_cuarto.png" kind="photo"
                  w={440} h={264} x={slotB2} y={40} z={0} ry={-10} rx={2}
                  lit={1} litColor={V.amber}
                  label="UN CUARTO, NO LA CASA" sheenAt={toCF(1150)} radius={12} />
              </Plane>
            )}
            {g >= S_C && g < S_D + RIEL_DUR && (
              <Plane z={190}>
                <MediaCard src="broll/cmepanel30/cmep30_s12_clip_enrosca_led.mp4" kind="video"
                  w={440} h={264} x={slotC} y={40} z={0} ry={-10} rx={2}
                  startFrom={2} lit={1} litColor={V.white}
                  label="ENROSCAR EL LED" sheenAt={toCF(1252)} radius={12} />
              </Plane>
            )}
            {g >= S_D && g < S_OUT + RIEL_DUR && (
              <Plane z={190}>
                <MediaCard src="broll/cmepanel30/cmep30_s12_clip_sube_termostato.mp4" kind="video"
                  w={440} h={264} x={slotD} y={40} z={0} ry={-10} rx={2}
                  startFrom={2} lit={1} litColor={V.amber}
                  label="SUBIR EL TERMOSTATO" sheenAt={toCF(1372)} radius={12} />
              </Plane>
            )}

            {/* la segunda tarjeta, abajo a la derecha: las resistencias al rojo primero y los diez
                focos con sus diez LED después. Material real otra vez, nunca forma con texto. */}
            {g >= 900 && g < 992 && (
              <Plane z={170}>
                <MediaCard src="img/cmepanel30/cmep30_s12_estufa_resistencias.png" kind="photo"
                  w={lerp(290, 330, es(clamp01((g - 900) / 56)))}
                  h={lerp(174, 198, es(clamp01((g - 900) / 56)))}
                  x={lerp(112, 80, es(clamp01((g - 900) / 32)))}
                  y={lerp(80, 76, es(clamp01((g - 900) / 56)))}
                  z={0} ry={-12} rx={3} lit={1} litColor={V.danger}
                  label="LA QUE MÁS COME" sheenAt={toCF(952)} radius={10} />
              </Plane>
            )}
            {g >= 1226 && g < 1330 && (
              <Plane z={170}>
                <MediaCard src="img/cmepanel30/cmep30_s12_diez_focos_mesa.png" kind="photo"
                  w={lerp(300, 344, es(clamp01((g - 1226) / 60)))}
                  h={lerp(180, 206, es(clamp01((g - 1226) / 60)))}
                  x={lerp(112, 80, es(clamp01((g - 1226) / 34)))}
                  y={lerp(78, 74, es(clamp01((g - 1226) / 60)))}
                  z={0} ry={-12} rx={3} lit={0.94} litColor={V.white}
                  label="DIEZ Y DIEZ" sheenAt={toCF(1286)} radius={10} />
              </Plane>
            )}
          </AbsoluteFill>
        )}

        {/* ═══ ACTO 4 · "45 DÓLARES AL MES" ══════════════════════════════════════════════════
            Sube CÁLIDO desde abajo: es lo que TE QUEDA, no lo que te cobran. Con la escuadra
            abierta (1448-1546) nada de esto entra en la franja (y > 66 %) ni en el brazo
            (x 12-28 % · y 26-66 %). ═══════════════════════════════════════════════════════ */}
        {g >= A4 - 6 && g < 1664 && (
          <>
            <Plane z={240}>
              {g >= A4 && g < 1584 && (() => {
                const fl = flujo("queda", clamp01((g - A4) / 26));
                return (
                  <div style={{
                    transform: `translateY(${(fl.dy * 0.5).toFixed(1)}px)`,
                    opacity: fl.opacity * clamp01(1 - (g - 1560) / 20),
                  }}>
                    <Readout value="45" label="EN UNA CASA COMÚN · AL MES" at={toCF(A4 + 3)}
                      x={interpolate(g, [A4, 1490, 1560], [50, 50, 44], CL)}
                      y={interpolate(g, [A4, 1560], [46, 42], CL)}
                      size={interpolate(g, [A4, 1470, 1560], [232, 186, 176], CL)} color={V.amber} />
                  </div>
                );
              })()}
              {g >= 1444 && g < 1572 && (
                <IconPng src="img/cmepanel30/cmep30_ic_calculadora.png" x={76} y={50} size={118} z={0}
                  opacity={0.88 * clamp01((g - 1444) / 14) * clamp01(1 - (g - 1556) / 14)}
                  rot={-5} glow={V.ink0} />
              )}
            </Plane>

            {/* ⭐ EL REMATE GENEROSO. Sobrevive la frontera D y muere ya adentro del acto 5: la
                idea cierra del otro lado del cartón. ⛔ Sin la cifra del panel: es un precio. */}
            <Plane z={250}>
              <Cartel g={g} at={1492} out={1630} x={42} y={16} size={46} color={V.white}>
                ES MÁS DE LO QUE<br />ME AHORRA <span style={{ color: V.volt }}>MI PROPIO PANEL</span>
              </Cartel>
            </Plane>

            {/* el panel, chiquito y frío arriba a la derecha: el objeto que perdió la discusión */}
            {g >= 1466 && g < 1546 && (
              <Plane z={180}>
                <MediaCard src="img/cmepanel30/cmep30_s11_un_solo_panel.png" kind="photo"
                  w={lerp(292, 330, es(clamp01((g - 1466) / 52)))}
                  h={lerp(176, 198, es(clamp01((g - 1466) / 52)))}
                  x={lerp(114, 78, es(clamp01((g - 1466) / 30)))}
                  y={lerp(30, 27, es(clamp01((g - 1466) / 52)))}
                  z={0} ry={-11} rx={3} lit={0.8} litColor={V.sky}
                  label="MI KIT" sheenAt={toCF(1512)} radius={10} />
              </Plane>
            )}
          </>
        )}

        {/* ═══ ACTO 5 · EL CUADERNO Y EL QR ══════════════════════════════════════════════════
            "Lo dejé abajo por si te sirve." NADA de este acto entra nunca en la caja del QR
            (x 60-87 % · y 32-83 %): todo ocurre en la mitad izquierda del cuadro. ══════════ */}
        {g >= SW5 - 24 && (
          <>
            {/* él juntando los tres cuadernillos: la materia del wipe, ya aterrizada */}
            {g >= 1596 && g < 1730 && (
              <Plane z={180}>
                <MediaCard src="broll/cmepanel30/cmep30_s12_clip_apila_manuales.mp4" kind="video"
                  w={lerp(430, 476, es(clamp01((g - 1596) / 76)))}
                  h={lerp(258, 286, es(clamp01((g - 1596) / 76)))}
                  x={lerp(-16, 27, es(clamp01((g - 1596) / 38)))}
                  y={lerp(36, 32, es(clamp01((g - 1596) / 70)))}
                  z={0} ry={12} rx={-2} startFrom={4} lit={1} litColor={V.amber}
                  label="PASADO EN LIMPIO" sheenAt={toCF(1660)} radius={12} />
              </Plane>
            )}

            {/* la única idea de texto del acto. Nada de "conseguí", nada de "aprovechá". */}
            <Plane z={240}>
              <Cartel g={g} at={1650} out={1768} x={30} y={72} size={56} color={V.white}>
                LO DEJÉ ABAJO<br />POR SI <span style={{ color: V.amber }}>TE SIRVE</span>
              </Cartel>
              <Rotulo x={30} y={72} size={52} color={V.white}
                op={clamp01((g - 1790) / 14) * clamp01(1 - (g - 1868) / 8)}>
                SIN COMPRAR <span style={{ color: V.volt }}>NADA</span>
              </Rotulo>
            </Plane>

            {/* MATERIA SALIENTE: el cuaderno CERRÁNDOSE sobre la mesa. La tarjeta se acuesta
                (rx 2° → 58°), se achica y su sombra de contacto se aprieta contra la madera. */}
            {g >= 1772 && (
              <Plane z={160}>
                <MediaCard src="img/cmepanel30/cmep30_s12_lam_cuaderno_pasado_limpio.png" kind="photo"
                  w={lerp(430, 348, es(clamp01((g - 1772) / 92)))}
                  h={lerp(258, 209, es(clamp01((g - 1772) / 92)))}
                  x={lerp(-14, 26, es(clamp01((g - 1772) / 40)))}
                  y={lerp(40, 44, es(clamp01((g - 1772) / 92)))}
                  z={0} ry={10} rx={lerp(2, 58, es(clamp01((g - 1796) / 74)))}
                  lit={0.92} litColor={V.amber}
                  label="EL CUADERNO" sheenAt={toCF(1812)} radius={12} />
              </Plane>
            )}
          </>
        )}
      </Layers>

      {/* ════ LAS COSTURAS, siempre por encima de todo ════════════════════════════════════════
          A @423  · MATCH-SHAPE   — vive adentro del objeto (la superficie que se repinta).
          B @817  · OCLUSIÓN      — la hoja tapa el 100 % entre 814 y 823.
          C @1427 · ZOOM-THROUGH  — vive en la cámara (`zoomThrough`): no hay overlay.
          D @1548 · WIPE POR MATERIA — los tres cuadernillos de cartón cruzan de canto. */}
      <OcluyeHoja at={800} dur={32} />
      <Cuadernillos at={A5} dur={44} />

      {/* el polvo del taller contra la luz del portón. Se apaga en 1560: a partir de ahí NADA
          vuelve a montarse por encima, para que el QR quede absolutamente limpio. */}
      {g < 1560 && (
        <AbsoluteFill style={{ pointerEvents: "none", opacity: 0.42 }}>
          {Array.from({ length: 11 }, (_, i) => {
            const o = rnd(i * 6.3);
            const yy = ((rnd(i * 4.1) * 100 - (g * (0.2 + o * 0.36)) / 9) % 108 + 108) % 108;
            return (
              <div key={i} style={{
                position: "absolute", left: `${(4 + rnd(i * 8.7) * 92).toFixed(1)}%`, top: `${yy.toFixed(1)}%`,
                width: 1.5, height: 26 + o * 64,
                background: `linear-gradient(180deg, rgba(0,0,0,0), ${rgba(V.torch, 0.07 + o * 0.08)}, rgba(0,0,0,0))`,
              }} />
            );
          })}
        </AbsoluteFill>
      )}

      {/* ══════════════════════════════════════════════════════════════════════════════════════
          ⭐ EL QR — el ÚLTIMO nodo del árbol. Nada puede quedar por encima.
          Es un OBJETO APOYADO EN LA MADERA: tiene su sombra de contacto (debajo) y el filo del
          papel, y nada más. ⛔ Fuera de `Layers` → sin perspectiva, sin rotación, sin escala
          animada, sin blur, sin grade, sin deriva. 470 px = 43,5 % del alto del cuadro; la caja
          va de x 1186-1656 px (61,8-86,3 %) e y 359-829 px (33,2-76,8 %).
          VENTANA LIMPIA GARANTIZADA: 1596 → 1875 = 279 frames (9,30 s).
          Se monta en 1548 DETRÁS del wipe de cartón: el cartón lo DESCUBRE ya apoyado en la mesa,
          no lo trae volando. Un objeto en reposo no tiembla — el hold vivo del acto lo ponen la
          cama, la luz, el polvo previo y el cuaderno que se cierra a la izquierda. ════════════ */}
      {g >= QR_MOUNT && (
        <>
          {/* la sombra de contacto sobre la veta del banco: va DEBAJO, nunca encima */}
          <div style={{
            position: "absolute", left: 1150, top: 802, width: 542, height: 76,
            borderRadius: "50%",
            background: `radial-gradient(closest-side, ${rgba(MADERA, 0.86)}, rgba(0,0,0,0) 78%)`,
          }} />
          <div style={{
            position: "absolute", left: 1186, top: 359, width: QR_PX, height: QR_PX,
            backgroundColor: "#FFFFFF",
            border: `1px solid ${rgba(V.ink0, 0.14)}`,
            boxShadow: `0 26px 62px ${rgba(V.ink0, 0.72)}, 0 6px 18px ${rgba(V.ink0, 0.6)}`,
          }}>
            <Img src={staticFile("img/cmepanel30/cmep30_qrcard.png")}
              style={{ width: QR_PX, height: QR_PX, display: "block" }} />
          </div>
          {/* el rótulo del objeto: va DEBAJO de la caja, jamás encima. Y no dice ni precio ni
              dirección: dice de qué es la hoja que hay del otro lado. */}
          <div style={{
            position: "absolute", left: 1186, top: 846, width: QR_PX, textAlign: "center",
            opacity: clamp01((g - QR_CLEAN - 12) / 20),
            fontFamily: F_BODY, fontWeight: 600, fontSize: 30, letterSpacing: 2.2,
            color: rgba(V.bone, 0.82), textTransform: "uppercase",
            textShadow: "0 4px 18px rgba(0,0,0,0.95)",
          }}>
            LAS 14 · ORDENADAS
          </div>
        </>
      )}
    </AbsoluteFill>
  );
};
